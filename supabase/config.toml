project_id = "dog-adopt"

[api]
enabled = true
port = 54321
schemas = ["dogadopt"]
extra_search_path = ["dogadopt"]
max_rows = 1000

[db]
port = 54322
major_version = 15

[auth]
enabled = true
site_url = "http://localhost:8080"
additional_redirect_urls = ["http://localhost:8080", "http://localhost:8081"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10
enable_signup = true

[auth.email]
enable_signup = true
double_confirm_changes = true
enable_confirmations = false

# Google OAuth is disabled for local development
# Enable and configure in production deployment
# [auth.external.google]
# enabled = true
# client_id = "your-google-client-id"
# secret = "your-google-client-secret"